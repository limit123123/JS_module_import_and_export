export const uplodaurl = "/upload_file";

const obj = {
  name: "Tom",
  age: 18
};

export const { name, age } = obj;

const sum = (a, b) => a + b;

export function plus() {
  return "这里是plus方法";
}

export default sum;
